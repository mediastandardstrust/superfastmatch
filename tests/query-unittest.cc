#include <tests.h>

TEST(DocumentQueryTest,ParsingTest){
  MockRegistry registry;
  // DocumentQuery query(&registry,"");
  // vector<DocPair> docs;
  // EXPECT_FALSE(query.getOrderedDocPairs(docs));
  // EXPECT_EQ(0U,docs.size());
}